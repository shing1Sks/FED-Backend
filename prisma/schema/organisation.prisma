model Organisation {
    id          String   @id @default(cuid()) @map("_id")
    name        String
    description String
    events      Event[]
    createdAt   DateTime @default(now())
    updatedAt   DateTime @updatedAt
}
